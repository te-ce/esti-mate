export enum User {
  CONNECTED = "connected",
  DISCONNECTED = "disconnected",
}

export enum Room {
  UPDATE = "rooms:update",
}

export enum Submit {
  ESTIMATION = "submit:estimation",
  NEXT = "submit:next",
}
